source 'https://rubygems.org'


ruby '>= 2.7'


# Optional UI gems — recommended for a nicer terminal UX
gem 'artii' # ASCII art banners
gem 'pastel' # coloring
gem 'tty-box' # boxed panels
gem 'tty-spinner' # spinners while connecting


# Note: mpg123 is still required externally (system package). These gems only improve the UI.